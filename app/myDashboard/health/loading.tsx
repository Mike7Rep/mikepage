import { DashboardLoadingFrame } from "../_components/dashboard-loading-frame"

export default function Loading() {
  return <DashboardLoadingFrame activeSection="health" subtitle="Health wird geladen." />
}
