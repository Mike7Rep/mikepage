services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: mikepage
      POSTGRES_USER: mikepage
      POSTGRES_PASSWORD: mikepage
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: sh -c "pnpm install --frozen-lockfile && pnpm prisma migrate deploy && pnpm dev --hostname 0.0.0.0"
    depends_on:
      - db
    env_file:
      - path: .env
        required: false
      - path: .env.local
        required: false
    environment:
      CI: "true"
      DATABASE_URL: postgresql://mikepage:mikepage@db:5432/mikepage
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - web-node-modules:/app/node_modules
      - web-next:/app/.next

volumes:
  postgres-data:
  web-node-modules:
  web-next:
