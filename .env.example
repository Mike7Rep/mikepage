ALPACA_ENDPOINT=https://api.alpaca.markets
ALPACA_KEY=
ALPACA_SECRET=
ALPACA_DATA_ENDPOINT=https://data.alpaca.markets

MYDASHBOARD_USER=mike.rep@me.com
MYDASHBOARD_PASSWORD=
MYDASHBOARD_SESSION_SECRET=
REDIS_URL=redis://<user>:<password>@<public-railway-host>:<port>

DATABASE_URL=postgresql://<user>:<password>@<public-railway-host>:<port>/<database>
DIRECT_URL=postgresql://<user>:<password>@<public-railway-host>:<port>/<database>

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_KEY=
# Optional. Falls leer, wird die aktuelle Request-Origin verwendet.
GOOGLE_HEALTH_REDIRECT_URI=

WITHINGS_CLIENT_ID=
WITHINGS_CLIENT_SECRET=
# Muss exakt mit der Callback URL im Withings-Partnerportal übereinstimmen.
WITHINGS_REDIRECT_URI=https://www.michael-repolusk.com/myDashboard/withings/callback

# Local development uses Railway's public *.proxy.rlwy.net endpoints.
# Never use *.railway.internal outside Railway.
# This configuration points at production data: do not migrate, reset or seed casually.

# Railway production:
# - Web service: set DATABASE_URL from Postgres, REDIS_URL from Redis and all dashboard variables here.
